# %%
import numpy as np

prevLayerSize = 30
layerSize = 20

prevLayerValues = np.random.rand(prevLayerSize)
weights = np.random.rand(prevLayerSize,layerSize)
biases = np.random.rand(prevLayerSize)

# %%
